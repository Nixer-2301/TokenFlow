Run `tokenflow init --generate-prompt` to create the default 20,000+ token
long-context prompt. The generated prompt asks the model for a response over
10,000 completion tokens when the provider allows it.
